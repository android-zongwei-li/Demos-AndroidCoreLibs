这个模块负责提供其他模块都可能用到的东西。
上层模块有共性的东西，就应该抽取到common中。
比如日志、通用的数据结构（bean类或者data类）、模拟数据等。

目前此模块包括：
1、bean
整个应用公用的数据结构，应该抽取到这里，避免各个模块重复造轮子。

2、data
用来存一些可能写demo会用到的数据。看数据量多不多，后面如果数据较多，可以单独作为一个模块。

3、log
日志框架。